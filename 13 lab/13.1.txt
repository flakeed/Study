#include <iostream>

using namespace std;
int main() {
    int arr[4][6];
    double v[6];
    int s = 0;
    for (int i = 0; i < 4; ++i) {
        for (int j = 0; j < 6; ++j) {
            arr[i][j] = rand() % 15;
            if (i == j) s = s + arr[i][j];
            cout << arr[i][j] << "\t";
        }
        cout << "\n";
    }
    int sum = 0;
    int k = 1;
    for (int j = 0; j < 6; j++) {
        for (int i = 0; i < 4; i++) {
	        if (arr[i][j]>0 && arr[i][j]%3==0)
	        {
                sum += arr[i][j];
                k++;
	        }
        }
    }
    
       cout << "sum is: "  << sum;
    cout << "num is: " << k;

/*Обчислити суму та кількість елементів матриці, кратних 3*/
    return 0;
}
